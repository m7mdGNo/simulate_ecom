services:
  django:
    build: .
    restart: always
    command: bash -c "python manage.py migrate && python manage.py collectstatic --noinput && python manage.py createsuperuser --noinput || true && gunicorn --workers 8 --bind 0.0.0.0:8000 --preload project.wsgi:application"
    container_name: simulate_ecom
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/farm_ecommerce
    ports:
      - "8001:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/products/"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 20s

  prometheus:
    image: prom/prometheus:v2.54.1
    container_name: simulate_ecom_prometheus_prod
    restart: always
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    depends_on:
      - django

  grafana:
    image: grafana/grafana:11.2.2
    container_name: simulate_ecom_grafana_prod
    restart: always
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
    volumes:
      - ./monitoring/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
      - ./monitoring/grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./monitoring/grafana/dashboards:/etc/grafana/dashboards:ro
      - grafana_data_prod:/var/lib/grafana
    ports:
      - "3000:3000"
    depends_on:
      - prometheus

  db:
    image: postgres:16-alpine
    restart: always
    container_name: simulate_ecom_db
    environment:
      POSTGRES_DB: farm_ecommerce
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d farm_ecommerce"]
      interval: 5s
      timeout: 5s
      retries: 10

  simulate:
    build: .
    command: bash -c "API_URL=http://django:8000/api python simulate.py"
    depends_on:
      django:
        condition: service_healthy

volumes:
  postgres_data:
  grafana_data_prod:
